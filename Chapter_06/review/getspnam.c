#include <stdio.h>
#include <stdlib.h>
	if (argc != 2) {
		printf("./a.out name\n");
		exit(1);
	}
	

		exit(1);
	}
	printf("sp_namp = %s\n"
		   "sp_pwdp = %s\n"
		   "sp_lstchg = %d\n",
		   pwd->sp_namp,
		   pwd->sp_pwdp,
		   pwd->sp_lstchg);
	
	return 0;	
}